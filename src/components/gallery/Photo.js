import React from 'react';

const FlickrImages = props => {
    var photos = props.photos
    return (
        <li key={id}>
            <img src={} alt="" />
        </li>
    )
}

export default FlickrImages;